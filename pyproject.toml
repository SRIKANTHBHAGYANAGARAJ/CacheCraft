[project]
name = "cachecraft"
version = "0.8.0"
description = "Semantic Cache Strategy Bench"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}

[tool.cachecraft]
report-format = "json"

